package study03;

import java.util.Arrays;

public class Ascending {
	
	public int[] make(int[] arr) {
		Arrays.sort(arr);
		return arr;
	}
	
	public char[] make(char[] arr) {
		Arrays.sort(arr);
		return arr;
	}

}
